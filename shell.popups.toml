# Overrides [popups]; a section override replaces the whole block.
# Frosted only where the omarchy-keyboard-panel and omarchy-osd layers are blurred.
background       = "#0d0e10"
background-alpha = 0.8
text             = "#E1E1E1"
border           = "hyprland.active-border"
border-alpha     = 1.0
