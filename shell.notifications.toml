# Overrides [notifications]; a section override replaces the whole block.
# alpha 0.663 reproduces mako's #0d0e10A9.
background       = "#0d0e10"
background-alpha = 0.663
text             = "#E1E1E1"
border           = "hyprland.active-border"
border-alpha     = 1.0
countdown        = "#A2B5C1"
