# Overrides [tooltip]; a section override replaces the whole block.
# Frosted only via blur_popups on the omarchy-bar layer rule.
background       = "#0d0e10"
background-alpha = 0.8
text             = "#E1E1E1"
border           = "hyprland.active-border-foreground"
border-alpha     = 1.0
